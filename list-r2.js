const dotenv = require('dotenv'); dotenv.config({ path: '.env.local' }); const ACCOUNT_ID = process.env.CLOUDFLARE_ACCOUNT_ID; const API_TOKEN = process.env.CLOUDFLARE_API_TOKEN; const BUCKET = process.env.CLOUDFLARE_R2_BUCKET; async function list() { const res = await fetch('https://api.cloudflare.com/client/v4/accounts/' + ACCOUNT_ID + '/r2/buckets/' + BUCKET + '/objects', { headers: { Authorization: 'Bearer ' + API_TOKEN } }); const json = await res.json(); console.log((json.result || []).map(o => o.key + ' (' + o.size + ')').filter(k => k.toLowerCase().includes('myrik')).join('\n')); } list();
